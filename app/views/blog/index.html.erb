<div class="uk-container uk-container-center">
	<h1>Блог</h1>

	<% @posts.each do |p| %>
		<div class="uk-panel uk-panel-box">
			<h2><%= p.title %></h2>
			<h6><%= p.created_at %></h6>
			<p><%= p.text %></p>
		</div>
	<% end %>
</div>